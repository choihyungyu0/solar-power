# 99 — Final review

Read `AGENTS.md` and `docs/CODEX_HARNESS.md` first.

Do a final review of the MVP.

Check:

1. `npm.cmd run build` passes.
2. No Supabase service_role key or secret is committed.
3. `.env.local` is ignored.
4. UI labels are Korean and beginner-friendly.
5. Estimates are clearly labeled as demo/reference values.
6. FastAPI was not changed unless explicitly required.
7. The app can run locally even if Supabase env variables are missing.

Fix small issues if needed, then report:

1. Files changed
2. Build result
3. Remaining manual setup steps
4. Recommended next task
