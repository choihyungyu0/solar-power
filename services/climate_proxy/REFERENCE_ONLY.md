# 이 폴더는 참고 구현입니다 (운영 채택 아님)

본 FastAPI 패키지는 **"분석가가 백엔드 운영까지 책임지는 안"** 을 시도하다가 분석가 능력 범위를 벗어나 **참고 자료로 강등**된 코드입니다.

## 누가 보면 좋은가
- 개발자(최현규)가 climate.gg 5개 API 를 묶는 백엔드를 **직접 만들 때** Python 참고 구현으로 활용
- 8단계 파이프라인의 캐시·rate limit·CORS 처리 방식 참고

## 누가 보지 않아도 되는가
- 분석가(양인성) — 본 코드의 운영·디버깅 책임 없음
- 개발자가 다른 방식(TypeScript Vercel function 등)을 선택할 경우 — 이 폴더 무시

## 실제 권장되는 데이터/API 전달은
→ `docs/HANDOFF_PACKAGE.md` 참조 (API 카탈로그 + mvp_app 예시 + 시드 fixture)
